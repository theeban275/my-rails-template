<%# add navigation links to this file %>
<% if user_signed_in? %>
  <% if current_user.admin? %>
    <li><%= button_to 'Users', users_path, method: :get, class: 'btn btn-link navbar-btn navbar-btn-left' %></li>
  <% end %>
<% end %>
<% if user_signed_in? %>
  <li><%= button_to 'Edit account', edit_user_registration_path, method: :get, class: 'btn btn-link navbar-btn navbar-btn-left' %></li>
  <li><%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: 'btn btn-link navbar-btn' %></li>
<% else %>
  <li><%= button_to 'Sign in', new_user_session_path, method: :get, class: 'btn btn-link navbar-btn navbar-btn-left' %></li>
  <li><%= button_to 'Sign up', new_user_registration_path, method: :get, class: 'btn btn-primary navbar-btn' %></li>
<% end %>
